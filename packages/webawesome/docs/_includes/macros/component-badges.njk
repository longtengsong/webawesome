{# Renders a single Stable or Experimental pill — safe to call inline in prose. #}
{%- macro statusBadge(status) -%}
{%- if status == 'stable' -%}
{# `.preview` opts the brand-variant badge out of site theming (orange). #}
<wa-badge class="preview" variant="brand" pill><wa-icon name="check" slot="start"></wa-icon>稳定</wa-badge>
{%- elif status == 'experimental' -%}
<wa-badge variant="warning" appearance="filled" pill><wa-icon name="flask" slot="start"></wa-icon>实验性</wa-badge>
{%- endif -%}
{%- endmacro -%}

{# Status + Since badges for a component. #}
{% macro componentStatusBadges(component) %}
{{ statusBadge(component.status) }}
{% if component.since %}
<wa-badge class="since" variant="neutral" appearance="filled" pill>自 {{ component.since }} 起</wa-badge>
{% endif %}
{% endmacro %}
