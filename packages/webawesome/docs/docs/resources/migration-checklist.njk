---
title: 迁移清单
description: 用于将项目从 Shoelace 2.x 迁移到 @SITE_NAME@ 的交互式清单。
layout: page
---

<wa-breadcrumb class="page-breadcrumbs">
  <wa-icon slot="separator" name="angle-right" variant="regular"></wa-icon>
  <wa-breadcrumb-item href="/docs/resources/migrating-from-shoelace">从 Shoelace 迁移</wa-breadcrumb-item>
  <wa-breadcrumb-item>{{ title }}</wa-breadcrumb-item>
</wa-breadcrumb>

<p>
  逐步跟踪你的迁移进度。你的进度会自动保存在浏览器的本地存储中，直到你重置它。此清单与 <a
    href="/docs/resources/migrating-from-shoelace">迁移指南</a> 采用相同的结构。如果需要任何项目的上下文，请在另一个标签页中打开指南。
</p>

<p>
  更喜欢可以直接放入仓库的版本？<a href="/assets/downloads/shoelace-migration-checklist.md"
    download="shoelace-migration-checklist.md">下载 Markdown 版本</a>。
</p>

<wa-card id="checklist-toolbar" class="checklist-toolbar wa-stack" role="toolbar" aria-label="迁移清单">
  <div class="checklist-toolbar-progress wa-cluster wa-gap-s" role="group" aria-labelledby="checklist-progress-label">
    <p id="checklist-progress-label" class="checklist-toolbar-count de-emphasize wa-font-weight-semibold">0 / 0
      已完成</p>
    <wa-progress-bar id="checklist-progress-bar" value="0" label="迁移进度"
      aria-labelledby="checklist-progress-label"></wa-progress-bar>
  </div>
  <div slot="footer" class="wa-grid">
    <wa-button id="checklist-reset" variant="danger" appearance="outlined" size="s">
      <wa-icon slot="start" name="arrow-rotate-left"></wa-icon>
      重置进度
    </wa-button>
    <wa-button href="/assets/downloads/shoelace-migration-checklist.md" download="shoelace-migration-checklist.md"
      variant="neutral" appearance="outlined" size="s">
      <wa-icon slot="start" name="download"></wa-icon>
      导出清单 (.md)
    </wa-button>
  </div>
</wa-card>

<section data-checklist-section="install">
  <h2>1. 安装 @SITE_NAME@</h2>

  <wa-checkbox data-checklist-item="install-removed-shoelace">从 <code>package.json</code> 中移除了 <code>@shoelace-style/shoelace</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-added-webawesome">向 <code>package.json</code> 中添加了 <code>@awesome.me/webawesome</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-css-import">已将 CSS 导入更新为 <code>@awesome.me/webawesome/dist/styles/webawesome.css</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-loader-import">已将自动加载器导入更新为 <code>@awesome.me/webawesome/dist/webawesome.loader.js</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-cdn-urls">已将 CDN URL（如果使用）更新为 <code>early.webawesome.com/webawesome@3/...</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-setbasepath">已将 <code>setBasePath</code> 导入更新为 <code>@awesome.me/webawesome/dist/webawesome.js</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-theme-picked">选择了起始主题：<code>wa-theme-shoelace</code> 用于软着陆，或 <code>default</code> / <code>awesome</code></wa-checkbox>
  <wa-checkbox data-checklist-item="install-build-passes">更换后应用构建成功</wa-checkbox>
</section>

<section data-checklist-section="global-replace">
  <h2>2. 全局查找和替换</h2>
  <p>按顺序运行这些操作。每轮之间进行提交。</p>

  <wa-checkbox data-checklist-item="replace-package">在导入语句中将 <code>@shoelace-style/shoelace</code> 替换为 <code>@awesome.me/webawesome</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-open-tag">在模板和 HTML 中将 <code>&lt;sl-</code> 替换为 <code>&lt;wa-</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-close-tag">在模板和 HTML 中将 <code>&lt;/sl-</code> 替换为 <code>&lt;/wa-</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-css-vars">在 CSS 文件、内联样式、CSS-in-JS 中将 <code>--sl-</code> 替换为 <code>--wa-</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-theme-class">替换了 <code>sl-theme-</code> 类名为 <code>wa-theme-</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-event-single">在事件监听器字符串中将 <code>'sl-</code> 替换为 <code>'wa-</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-event-double">在事件监听器字符串中将 <code>"sl-</code> 替换为 <code>"wa-</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-ts-event-class">在 TypeScript 事件类导入中将 <code>Sl</code> 替换为 <code>Wa</code>（例如 <code>SlChangeEvent</code> → <code>WaChangeEvent</code>）</wa-checkbox>
  <wa-checkbox data-checklist-item="replace-variant-primary">在所有组件上将 <code>variant="primary"</code> 替换为 <code>variant="brand"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-light-class">将 <code>class="sl-theme-light"</code> 替换为 <code>class="wa-light"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-dark-class">将 <code>class="sl-theme-dark"</code> 替换为 <code>class="wa-dark"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-prefix-slot">将 <code>slot="prefix"</code> 替换为 <code>slot="start"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-suffix-slot">将 <code>slot="suffix"</code> 替换为 <code>slot="end"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-helptext-attr">在表单控件上将 <code>help-text=</code> 替换为 <code>hint=</code></wa-checkbox>
  <wa-checkbox data-checklist-item="replace-helptext-slot">在表单控件上将 <code>slot="help-text"</code> 替换为 <code>slot="hint"</code></wa-checkbox>

  <h3>最后扫描：确认无匹配项</h3>

  <wa-checkbox data-checklist-item="sweep-sl-prefix"><code>grep -r 'sl-' src/</code> 没有匹配到任何有意义的内容</wa-checkbox>
  <wa-checkbox data-checklist-item="sweep-sl-css-var"><code>grep -r '\-\-sl\-' src/</code> 无匹配项</wa-checkbox>
  <wa-checkbox data-checklist-item="sweep-shoelace-pkg"><code>grep -r '@shoelace-style/shoelace' .</code> 无匹配项</wa-checkbox>
  <wa-checkbox data-checklist-item="sweep-event-strings"><code>grep -rE "['\"]sl-[a-z]+['\"]" src/</code> 无匹配项（捕获事件监听器字符串）</wa-checkbox>
</section>

<section data-checklist-section="components">
  <h2>3. 组件</h2>

  <h3>重命名的元素</h3>

  <wa-checkbox data-checklist-item="rename-alert"><code>&lt;sl-alert&gt;</code> → <code>&lt;wa-callout&gt;</code>（内联使用；toast UX 现在是 Pro 版的 <code>&lt;wa-toast&gt;</code>）</wa-checkbox>
  <wa-checkbox data-checklist-item="rename-image-comparer"><code>&lt;sl-image-comparer&gt;</code> → <code>&lt;wa-comparison&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="rename-range"><code>&lt;sl-range&gt;</code> → <code>&lt;wa-slider&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="rename-menu"><code>&lt;sl-menu&gt;</code> + <code>&lt;sl-menu-item&gt;</code> → <code>&lt;wa-dropdown&gt;</code> + <code>&lt;wa-dropdown-item&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="rename-menu-label"><code>&lt;sl-menu-label&gt;</code> → <code>&lt;wa-divider&gt;</code> + 标题，或重新构建结构</wa-checkbox>

  <h3>已移除的元素</h3>

  <wa-checkbox data-checklist-item="removed-icon-button"><code>&lt;sl-icon-button&gt;</code> → 带有单个 <code>&lt;wa-icon&gt;</code> 子元素的 <code>&lt;wa-button&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="removed-radio-button"><code>&lt;sl-radio-button&gt;</code> → <code>&lt;wa-button-group&gt;</code> 中的 <code>&lt;wa-radio&gt;</code> 控件</wa-checkbox>
  <wa-checkbox data-checklist-item="removed-visually-hidden"><code>&lt;sl-visually-hidden&gt;</code> → 在任何元素上使用 <code>class="wa-visually-hidden"</code> 工具类</wa-checkbox>

  <h3>按钮</h3>

  <wa-checkbox data-checklist-item="button-outline">将 <code>outline</code> 布尔值替换为 <code>appearance="outlined"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="button-circle">替换了 <code>circle</code> 布尔值（当按钮只有一个 <code>&lt;wa-icon&gt;</code> 子元素时会自动检测）</wa-checkbox>
  <wa-checkbox data-checklist-item="button-text">将 <code>variant="text"</code> 替换为 <code>appearance="plain"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="button-caret">将 <code>caret</code> 替换为 <code>with-caret</code></wa-checkbox>
  <wa-checkbox data-checklist-item="button-slot-rename">已将 <code>slot="prefix"</code>/<code>"suffix"</code> 更新为 <code>slot="start"</code>/<code>"end"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="button-events-native">已将 <code>sl-blur</code>/<code>sl-focus</code> 监听器更新为原生的 <code>blur</code>/<code>focus</code></wa-checkbox>
  <wa-checkbox data-checklist-item="button-invalid-event">已将 <code>sl-invalid</code> 更新为 <code>wa-invalid</code></wa-checkbox>

  <h3>表单控件</h3>

  <wa-checkbox data-checklist-item="form-helptext-attr">将 <code>help-text</code> 特性重命名为 <code>hint</code></wa-checkbox>
  <wa-checkbox data-checklist-item="form-helptext-slot">将 <code>help-text</code> 插槽重命名为 <code>hint</code></wa-checkbox>
  <wa-checkbox data-checklist-item="form-prefix-suffix-slots">将 <code>prefix</code>/<code>suffix</code> 插槽重命名为 <code>start</code>/<code>end</code></wa-checkbox>
  <wa-checkbox data-checklist-item="form-clearable">将 <code>clearable</code> 重命名为 <code>with-clear</code>（input、select）</wa-checkbox>
  <wa-checkbox data-checklist-item="form-filled">将 <code>filled</code> 布尔值替换为 <code>appearance="filled"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="form-events-native">已将 <code>sl-change</code>/<code>sl-input</code>/<code>sl-blur</code>/<code>sl-focus</code> 更新为原生事件（无前缀）</wa-checkbox>
  <wa-checkbox data-checklist-item="form-clear-event">已更新 <code>sl-clear</code> 监听器（事件已移除；在清除后监听 <code>input</code>）</wa-checkbox>
  <wa-checkbox data-checklist-item="form-invalid">已将 <code>sl-invalid</code> 更新为 <code>wa-invalid</code></wa-checkbox>
  <wa-checkbox data-checklist-item="form-removed-formdata-shim">移除了基于 <code>formdata</code> 事件的表单序列化 shim（表单关联是原生的）</wa-checkbox>
  <wa-checkbox data-checklist-item="form-name-required">确认每个表单控件都有一个 <code>name</code> 特性（原生 <code>FormData</code> 所必需的）</wa-checkbox>

  <h3>滑块（来自 <code>&lt;sl-range&gt;</code>）</h3>

  <wa-checkbox data-checklist-item="slider-renamed">已将元素重命名为 <code>&lt;wa-slider&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="slider-tooltip">将 <code>tooltip</code> 特性替换为 <code>with-tooltip</code> + <code>tooltip-placement</code> + <code>tooltip-distance</code></wa-checkbox>
  <wa-checkbox data-checklist-item="slider-range">考虑使用新的 <code>range</code> 特性进行双滑块选择</wa-checkbox>

  <h3>内联标注（来自 <code>&lt;sl-alert&gt;</code>）</h3>

  <wa-checkbox data-checklist-item="callout-replace-static">将 <code>&lt;sl-alert&gt;</code> 静态用法替换为 <code>&lt;wa-callout&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="callout-removed-attrs">移除了 <code>open</code>、<code>closable</code>、<code>duration</code>（标注始终渲染）</wa-checkbox>
  <wa-checkbox data-checklist-item="callout-removed-methods">移除了 <code>show()</code>/<code>hide()</code> 调用（使用条件渲染）</wa-checkbox>
  <wa-checkbox data-checklist-item="callout-toast-migration">将 <code>.toast()</code> 调用迁移到 @SITE_NAME@ Pro 的 <code>&lt;wa-toast&gt;</code>（或自行构建）</wa-checkbox>

  <h3>对话框和抽屉</h3>

  <wa-checkbox data-checklist-item="dialog-show-hide-events">已将 <code>sl-show</code>/<code>sl-hide</code>/<code>sl-after-show</code>/<code>sl-after-hide</code> 更新为 <code>wa-</code> 前缀</wa-checkbox>
  <wa-checkbox data-checklist-item="dialog-initial-focus">已将 <code>sl-initial-focus</code> 更新为 <code>wa-initial-focus</code></wa-checkbox>
  <wa-checkbox data-checklist-item="dialog-request-close">将 <code>sl-request-close</code> 监听器迁移到可取消的 <code>wa-hide</code></wa-checkbox>
  <wa-checkbox data-checklist-item="dialog-noheader">将 <code>noHeader</code> 特性重命名为 <code>without-header</code></wa-checkbox>

  <h3>下拉菜单和菜单</h3>

  <wa-checkbox data-checklist-item="dropdown-combined">将 <code>&lt;sl-dropdown&gt;</code> + <code>&lt;sl-menu&gt;</code> 合并为 <code>&lt;wa-dropdown&gt;</code>（菜单已移除）</wa-checkbox>
  <wa-checkbox data-checklist-item="dropdown-item-renamed">已将 <code>&lt;sl-menu-item&gt;</code> 重命名为 <code>&lt;wa-dropdown-item&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="dropdown-caret">将触发按钮上的 <code>caret</code> 重命名为 <code>with-caret</code></wa-checkbox>
  <wa-checkbox data-checklist-item="dropdown-select-event">已将 <code>sl-select</code> 更新为 <code>wa-select</code></wa-checkbox>
  <wa-checkbox data-checklist-item="dropdown-menu-label">替换了 <code>&lt;sl-menu-label&gt;</code> 分隔符（使用 <code>&lt;wa-divider&gt;</code> + 标题）</wa-checkbox>

  <h3>标签页</h3>

  <wa-checkbox data-checklist-item="tabs-show-event">已将 <code>sl-tab-show</code> 更新为 <code>wa-tab-show</code></wa-checkbox>
  <wa-checkbox data-checklist-item="tabs-hide-event">已将 <code>sl-tab-hide</code> 更新为 <code>wa-tab-hide</code></wa-checkbox>
  <wa-checkbox data-checklist-item="tabs-close-event">已将 <code>sl-close</code>（可关闭标签页）更新为使用新模式（详见文档）</wa-checkbox>

  <h3>树形</h3>

  <wa-checkbox data-checklist-item="tree-selection-change">已将 <code>sl-selection-change</code> 更新为 <code>wa-selection-change</code></wa-checkbox>
  <wa-checkbox data-checklist-item="tree-expand-collapse">已将 <code>sl-expand</code>/<code>sl-collapse</code>/<code>sl-after-expand</code>/<code>sl-after-collapse</code> 更新为 <code>wa-</code> 前缀</wa-checkbox>
  <wa-checkbox data-checklist-item="tree-lazy-events">已将 <code>sl-lazy-load</code>/<code>sl-lazy-change</code> 更新为 <code>wa-lazy-load</code>/<code>wa-lazy-change</code></wa-checkbox>

  <h3>标签</h3>

  <wa-checkbox data-checklist-item="tag-removable">将 <code>removable</code> 重命名为 <code>with-remove</code></wa-checkbox>
  <wa-checkbox data-checklist-item="tag-remove-event">已将 <code>sl-remove</code> 更新为 <code>wa-remove</code></wa-checkbox>

  <h3>其他组件（仅事件名称）</h3>

  <wa-checkbox data-checklist-item="other-events">已将所有 <code>sl-*</code> 事件更新为 <code>wa-*</code>（或适用时使用原生事件）</wa-checkbox>
  <wa-checkbox data-checklist-item="other-confirmed-no-other-changes">除前缀替换外，这些组件没有其他 API 变更</wa-checkbox>

  <h3>图标</h3>

  <wa-checkbox data-checklist-item="icons-decision">已决定：继续使用 Bootstrap Icons（注册为自定义库）<strong>或</strong>切换到 Font Awesome</wa-checkbox>
  <wa-checkbox data-checklist-item="icons-renamed">如果切换：已更新图标名称（例如 <code>exclamation-triangle</code> → <code>triangle-exclamation</code>）</wa-checkbox>
  <wa-checkbox data-checklist-item="icons-fa-pro">如果使用 Font Awesome Pro：已配置 <code>setKitCode()</code> 或 <code>data-fa-kit-code</code></wa-checkbox>
</section>

<section data-checklist-section="theme-tokens">
  <h2>4. 主题令牌</h2>

  <wa-checkbox data-checklist-item="token-primary">将 <code>--sl-color-primary-*</code> 替换为 <code>--wa-color-brand-*</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-spacing">将 <code>--sl-spacing-*</code> 替换为 <code>--wa-space-{2xs,xs,s,m,l,xl,2xl,...}</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-font-size">将 <code>--sl-font-size-{x-small,small,medium,large,x-large,...}</code> 替换为 <code>--wa-font-size-{xs,s,m,l,xl,...}</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-font-mono">将 <code>--sl-font-mono</code> 替换为 <code>--wa-font-family-code</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-font-sans">将 <code>--sl-font-sans</code> 替换为 <code>--wa-font-family-body</code>（或 <code>--wa-font-family-heading</code>）</wa-checkbox>
  <wa-checkbox data-checklist-item="token-font-serif">将 <code>--sl-font-serif</code> 替换为 <code>--wa-font-family-longform</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-line-height-dense">将 <code>--sl-line-height-dense</code> 替换为 <code>--wa-line-height-condensed</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-line-height-loose">将 <code>--sl-line-height-loose</code> 替换为 <code>--wa-line-height-expanded</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-radius">将 <code>--sl-border-radius-{small,medium,large}</code> 替换为 <code>--wa-border-radius-{s,m,l}</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-shadow-s">合并了 <code>--sl-shadow-x-small</code>/<code>-small</code> → <code>--wa-shadow-s</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-shadow-l">合并了 <code>--sl-shadow-large</code>/<code>-x-large</code> → <code>--wa-shadow-l</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-transition-fast">合并了 <code>--sl-transition-x-fast</code>/<code>-fast</code> → <code>--wa-transition-fast</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-transition-normal">将 <code>--sl-transition-medium</code> 重命名为 <code>--wa-transition-normal</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-input">将 <code>--sl-input-*</code> 替换为 <code>--wa-form-control-*</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-surface-default">将 <code>--sl-color-neutral-0</code> 替换为 <code>--wa-color-surface-default</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-text-normal">将 <code>--sl-color-neutral-1000</code> 替换为 <code>--wa-color-text-normal</code></wa-checkbox>
  <wa-checkbox data-checklist-item="token-zindex-removed">移除了 <code>--sl-z-index-*</code> 引用（使用级联层和堆叠上下文）</wa-checkbox>
  <wa-checkbox data-checklist-item="token-color-scale">已调整色阶引用：Shoelace 50/100/.../950 → @SITE_NAME@ 95/90/.../05（明度，已反转）</wa-checkbox>
  <wa-checkbox data-checklist-item="token-fallback-cleanup">已审查所有 <code>var(--sl-*, fallback)</code> 调用，以确保备用值不会默默地掩盖遗漏的迁移</wa-checkbox>
</section>

<section data-checklist-section="forms">
  <h2>5. 表单和验证</h2>

  <wa-checkbox data-checklist-item="forms-removed-formdata-listeners">移除了提取 Shoelace 控件值的 <code>formdata</code> 事件监听器</wa-checkbox>
  <wa-checkbox data-checklist-item="forms-formdata-correct">确认 <code>new FormData(form)</code> 返回预期的字段</wa-checkbox>
  <wa-checkbox data-checklist-item="forms-checkvalidity">确认 <code>form.checkValidity()</code> 和 <code>form.reportValidity()</code> 包含所有控件</wa-checkbox>
  <wa-checkbox data-checklist-item="forms-reset">确认 <code>form.reset()</code> 重置所有 <code>&lt;wa-*&gt;</code> 控件</wa-checkbox>
  <wa-checkbox data-checklist-item="forms-custom-validity">迁移了 <code>setCustomValidity()</code> 调用（仍然有效）或移至 <code>customError</code> 特性</wa-checkbox>
  <wa-checkbox data-checklist-item="forms-invalid-event">已将 <code>sl-invalid</code> 监听器更新为 <code>wa-invalid</code></wa-checkbox>
  <wa-checkbox data-checklist-item="forms-pseudo-classes">验证了浏览器原生伪类（<code>:invalid</code>、<code>:valid</code>、<code>:required</code>）按预期应用</wa-checkbox>
</section>

<section data-checklist-section="silent-breakage">
  <h2>6. 隐性破损风险</h2>

  <wa-checkbox data-checklist-item="silent-event-strings">确认没有剩余的 <code>addEventListener('sl-...')</code> 字符串</wa-checkbox>
  <wa-checkbox data-checklist-item="silent-prefix-suffix">确认没有剩余的 <code>slot="prefix"</code> 或 <code>slot="suffix"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="silent-helptext">确认没有剩余的 <code>help-text=</code> 特性</wa-checkbox>
  <wa-checkbox data-checklist-item="silent-variant-primary">确认没有剩余的 <code>variant="primary"</code></wa-checkbox>
  <wa-checkbox data-checklist-item="silent-css-parts">确认 CSS <code>::part()</code> 选择器在重命名后仍然定位到有效的部分</wa-checkbox>
  <wa-checkbox data-checklist-item="silent-dark-mode">确认深色模式仍然有效（<code>wa-dark</code> 类，而不是 <code>sl-theme-dark</code>）</wa-checkbox>
  <wa-checkbox data-checklist-item="silent-icons">确认图标正确渲染（Font Awesome 目录 vs Bootstrap Icons 名称）</wa-checkbox>
</section>

<section data-checklist-section="test">
  <h2>7. 测试</h2>

  <wa-checkbox data-checklist-item="test-build">生产构建成功</wa-checkbox>
  <wa-checkbox data-checklist-item="test-typecheck">TypeScript 类型检查通过（捕获事件类导入拼写错误）</wa-checkbox>
  <wa-checkbox data-checklist-item="test-lint">Linter 通过</wa-checkbox>
  <wa-checkbox data-checklist-item="test-forms">遍历了应用中的每个表单；提交、重置、验证均正常工作</wa-checkbox>
  <wa-checkbox data-checklist-item="test-dialog-drawer">遍历了每个对话框和抽屉；打开、关闭、esc、覆盖层点击均正常工作</wa-checkbox>
  <wa-checkbox data-checklist-item="test-menus">遍历了每个下拉菜单、菜单、标签页、树形</wa-checkbox>
  <wa-checkbox data-checklist-item="test-light-dark">在浅色和深色模式下进行视觉检查</wa-checkbox>
  <wa-checkbox data-checklist-item="test-a11y">在关键页面上进行可访问性审计（焦点顺序、屏幕阅读器名称、键盘导航）</wa-checkbox>
  <wa-checkbox data-checklist-item="test-no-console-errors">页面加载时没有控制台错误</wa-checkbox>
  <wa-checkbox data-checklist-item="test-no-icon-warnings">没有关于未注册图标名称的控制台警告</wa-checkbox>
</section>

<section data-checklist-section="follow-ups">
  <h2>8. 可选后续步骤</h2>

  <wa-checkbox data-checklist-item="followup-theme-switch">最初采用了 <code>wa-theme-shoelace</code>？考虑切换到 <code>default</code> 或 <code>awesome</code></wa-checkbox>
  <wa-checkbox data-checklist-item="followup-utilities">尝试了新的工具类（<code>wa-cluster</code>、<code>wa-stack</code>、<code>wa-grid</code> 等）来替换自定义布局 CSS</wa-checkbox>
  <wa-checkbox data-checklist-item="followup-page">尝试了 <code>&lt;wa-page&gt;</code> 用于应用外壳布局</wa-checkbox>
  <wa-checkbox data-checklist-item="followup-popover">在你之前使用带丰富内容的 tooltip 的地方尝试了 <code>&lt;wa-popover&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="followup-number-input">尝试了 <code>&lt;wa-number-input&gt;</code> 而不是 <code>&lt;wa-input type="number"&gt;</code></wa-checkbox>
  <wa-checkbox data-checklist-item="followup-pro">评估了 @SITE_NAME@ Pro 的 <code>&lt;wa-toast&gt;</code>、<code>&lt;wa-combobox&gt;</code>、<code>&lt;wa-file-input&gt;</code>、图表、趋势图、额外主题</wa-checkbox>
</section>

<style>
  wa-card#checklist-toolbar {
    --spacing: var(--wa-space-s);
  }

  #checklist-toolbar.checklist-toolbar {
    position: sticky;
    /* 清除粘性 wa-page 头部和子头部，使工具栏不被覆盖。 */
    inset-block-start: calc(var(--header-height, 0px) + var(--subheader-height, 0px) + .25rem);
    z-index: 1;
    /* 轻微外扩，使工具栏看起来像独立的 UI 表面，而不是正文文本。 */
    margin-inline: calc(-1 * var(--wa-space-m));
    margin-block: var(--wa-space-l);
    padding-block: var(--wa-space-s);
    padding-inline: var(--wa-space-m);
    background-color: var(--wa-color-surface-default);
    border: var(--wa-border-width-s) solid var(--wa-color-surface-border);
    border-radius: var(--wa-border-radius-m);
    box-shadow: var(--wa-shadow-s);
  }

  #checklist-toolbar .checklist-toolbar-count {
    margin-block: 0;
    margin-inline: 0;
    flex: 0 0 auto;
    max-inline-size: 100%;
  }

  /* 进度行：对齐和换行来自 wa-cluster；只有进度条的 flex 尺寸是自定义的。 */
  #checklist-toolbar .checklist-toolbar-progress wa-progress-bar {
    flex: 1 1 12rem;
    min-inline-size: 12rem;
  }

  @media (max-inline-size: 36rem) {
    #checklist-toolbar .checklist-toolbar-progress {
      flex-direction: column;
      align-items: stretch;
    }

    #checklist-toolbar .checklist-toolbar-progress wa-progress-bar {
      flex: 1 1 auto;
      min-inline-size: 0;
      inline-size: 100%;
    }

    #checklist-toolbar .checklist-toolbar-actions {
      flex-direction: column;
      align-items: stretch;
    }

    #checklist-toolbar .checklist-toolbar-actions wa-button {
      inline-size: 100%;
    }

    /* 当操作移出 .checklist-toolbar-actions 时，工具栏底部使用 slot="footer" + wa-grid */
    #checklist-toolbar [slot='footer'] wa-button {
      inline-size: 100%;
    }
  }

  #content section[data-checklist-section] {
    margin-block: var(--wa-space-xl);
  }

  #content section[data-checklist-section] h3 {
    margin-block-start: var(--wa-space-l);
  }

  /* 全宽行，不覆盖 wa-checkbox 上的 host display:inline-flex。 */
  #content wa-checkbox[data-checklist-item] {
    box-sizing: border-box;
    inline-size: 100%;
    max-inline-size: 100%;
    margin-block: var(--wa-space-xs);
  }

  /* 长导入路径：nowrap 会强制巨大的主最小内容；换行而不是一行到底。 */
  #content wa-checkbox[data-checklist-item] code {
    overflow-wrap: anywhere;
    word-break: break-word;
  }

  /* 淡化已完成的项目，让用户一眼就能看到还剩什么。 */
  #content wa-checkbox[data-checklist-item][checked] {
    opacity: 0.55;
    transition: opacity var(--wa-transition-normal);
  }

  @media (prefers-reduced-motion: reduce) {
    #content wa-checkbox[data-checklist-item][checked] {
      transition: none;
    }
  }
</style>

<script type="module">
  const STORAGE_KEY = 'wa-migration-checklist-v1';
  const items = document.querySelectorAll('wa-checkbox[data-checklist-item]');
  const progressBar = document.getElementById('checklist-progress-bar');
  const progressLabel = document.getElementById('checklist-progress-label');
  const resetButton = document.getElementById('checklist-reset');

  const loadState = () => {
    try {
      return JSON.parse(localStorage.getItem(STORAGE_KEY)) ?? {};
    } catch {
      return {};
    }
  };

  const saveState = (state) => {
    try {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
    } catch {
      // 存储可能已禁用（隐私模式、配额）。静默失败；页面仍可作为会话本地清单工作。
    }
  };

  const updateProgress = () => {
    const total = items.length;
    const done = [...items].filter(item => item.checked).length;
    const pct = total === 0 ? 0 : Math.round((done / total) * 100);
    progressBar.value = pct;
    progressLabel.textContent = `${done} / ${total} 已完成 (${pct}%)`;
  };

  const restore = () => {
    const state = loadState();
    items.forEach(item => {
      const id = item.dataset.checklistItem;
      if (state[id]) item.checked = true;
    });
    updateProgress();
  };

  const onChange = (event) => {
    const item = event.target.closest('wa-checkbox[data-checklist-item]');
    if (!item) return;
    const state = loadState();
    const id = item.dataset.checklistItem;
    if (item.checked) {
      state[id] = true;
    } else {
      delete state[id];
    }
    saveState(state);
    updateProgress();
  };

  const onReset = () => {
    if (!confirm('重置所有清单进度？此操作无法撤销。')) return;
    items.forEach(item => { item.checked = false; });
    saveState({});
    updateProgress();
  };

  // @SITE_NAME@ 复选框异步升级。在读取状态之前等待它们。
  customElements.whenDefined('wa-checkbox').then(() => {
    restore();
    items.forEach(item => item.addEventListener('change', onChange));
  });

  resetButton.addEventListener('click', onReset);
</script>
