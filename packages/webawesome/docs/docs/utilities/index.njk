---
title: CSS 工具
description: 预设的 CSS 工具类，让您可以高效地为组件和原生元素自定义样式和布局。
layout: docs
hasBreadcrumbs: false
hasOutline: false
---

<p>
{% markdown %}
  {{ description }}
{% endmarkdown %}
</p>

{% markdown %}
与其一遍又一遍地手写相同的 CSS，不如使用工具类，您的主题会处理其余部分。CSS 工具直接从您的 Web Awesome [主题](/docs/themes)和[设计令牌](/docs/tokens)中提取，因此颜色、排版、圆角和间距与页面上的每个组件保持一致。它们与框架无关，适用于任何原生 HTML 元素，并可以自由组合。

## 使用 CSS 工具

CSS 工具作为 `webawesome.css` 的一部分提供，因此如果您已经包含该样式表，则无需额外导入。否则，请按照以下说明单独包含工具样式表。
{% endmarkdown %}

<wa-tab-group data-install-tabs>
  <wa-tab panel="cdn"><wa-icon name="globe" variant="regular"></wa-icon> CDN</wa-tab>
  <wa-tab panel="npm"><wa-icon name="box-open" variant="regular"></wa-icon> npm</wa-tab>
  <wa-tab panel="self-hosted"><wa-icon name="arrow-down-to-line" variant="regular"></wa-icon> 自托管</wa-tab>
  <wa-tab panel="hosted"><wa-icon name="rocket-launch" variant="regular"></wa-icon> 托管项目</wa-tab>

  <wa-tab-panel name="cdn">
{% markdown %}
将以下样式表添加到页面的 `<head>` 部分：

```html
<link rel="stylesheet" href="{% cdnUrl 'styles/utilities.css' %}">
```
{% endmarkdown %}
  </wa-tab-panel>

  <wa-tab-panel name="npm">
{% markdown %}
安装 `@awesome.me/webawesome` 后，将以下样式表导入到您的应用中：

```js
import '@awesome.me/webawesome/dist/styles/utilities.css';
```
{% endmarkdown %}
  </wa-tab-panel>

  <wa-tab-panel name="self-hosted">
{% markdown %}
将以下样式表添加到页面的 `<head>` 部分：

```html
<link rel="stylesheet" href="/dist/styles/utilities.css">
```
{% endmarkdown %}
  </wa-tab-panel>

  <wa-tab-panel name="hosted">
{% markdown %}
如果您使用的是[托管项目](/workspaces)，可以从项目设置中开启 CSS 工具。

1. 前往您项目的 <wa-icon name="gear" variant="regular"></wa-icon> **设置**。
2. 在 **特性** 旁边，选中 **CSS 工具** 复选框。
3. **保存更改**，使用您项目的任何地方都会立即更新。
{% endmarkdown %}
  </wa-tab-panel>
</wa-tab-group>

<wa-divider></wa-divider>

<h2 class="modern-card-list-heading">样式工具</h2>
{% markdown %}
用于自定义颜色、排版、圆角等的预设规则 &mdash; 与框架无关，可在任何元素上组合使用。
{% endmarkdown %}

<div class="modern-card-list">
  <section class="search-list-grid">
    {% for page in collections.styleUtilities | sort(false, false, 'data.title') %}
      <a class="hover-grow hover-emphasize-border" href="{{ page.url }}">
        <wa-card>
          <span class="page-name">{{ page.data.title }}</span>
          {% if page.data.description %}
            <p class="modern-card-summary">{{ page.data.description | inlineMarkdown | striptags | safe }}</p>
          {% endif %}
        </wa-card>
      </a>
    {% endfor %}
  </section>
</div>

<h2 class="modern-card-list-heading">布局工具</h2>
{% markdown %}
命名布局如 [`wa-stack`](/docs/utilities/stack)、[`wa-cluster`](/docs/utilities/cluster)、[`wa-split`](/docs/utilities/split)、[`wa-flank`](/docs/utilities/flank)、[`wa-frame`](/docs/utilities/frame) 和 [`wa-grid`](/docs/utilities/grid) &mdash; 加上用于间距、对齐和换行的属性级辅助工具 &mdash; 无需自定义 CSS 即可响应式地组织内容。
{% endmarkdown %}

<div class="modern-card-list">
  <section class="search-list-grid">
    {% for page in collections.layoutUtilities | sort(false, false, 'data.title') %}
      <a class="hover-grow hover-emphasize-border" href="{{ page.url }}">
        <wa-card>
          <span class="page-name">{{ page.data.title }}</span>
          {% if page.data.description %}
            <p class="modern-card-summary">{{ page.data.description | inlineMarkdown | striptags | safe }}</p>
          {% endif %}
        </wa-card>
      </a>
    {% endfor %}
  </section>
</div>

<style>
  wa-tab-group + wa-divider {
    margin-block: 1rem 3rem;
  }
</style>
