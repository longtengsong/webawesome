---
title: 设计令牌
description: 控制每个 @SITE_NAME@ 组件外观的 CSS 自定义属性，为您的主题提供一致且可定制的外观。
layout: page-outline
wide: true
hasBreadcrumbs: false
hasOutline: false
override:tags: []
---

{% markdown %}
设计令牌是您的[主题](/docs/themes)的构建基石，贯穿每个 @SITE_NAME@ 组件，为您的项目赋予统一的外观和感觉。它们控制从排版和颜色到间距和边框的一切。
{% endmarkdown %}

<div class="modern-card-list">
  <section class="search-list-grid">
    {% for page in collections.tokens | sort(false, false, 'data.title') %}
      <a class="hover-grow hover-emphasize-border" href="{{ page.url }}">
        <wa-card>
          <span class="page-name">{{ page.data.title }}</span>
          {% if page.data.description %}
            <p class="modern-card-summary">{{ page.data.description | inlineMarkdown | striptags | safe }}</p>
          {% endif %}
        </wa-card>
      </a>
    {% endfor %}
  </section>
  <div class="search-list-empty" hidden>
    未找到结果
  </div>
</div>

<wa-divider style="--spacing: var(--wa-space-3xl);"></wa-divider>

{% markdown %}
## 使用 {{ title }}

设计令牌是以 `--wa-` 为前缀的 CSS 自定义属性。在自己的样式表中使用时，将令牌名称包裹在 CSS `var()` 函数中并赋值给属性：

```css
.branded-container {
  background-color: var(--wa-color-brand-fill-normal);
  border: var(--wa-border-width-s) var(--wa-border-style) var(--wa-color-brand-border-normal);
  color: var(--wa-color-brand-on-normal);
}
```

您也可以将设计令牌用于内联样式：

```html
<span style="
  background-color: var(--wa-color-brand-fill-normal);
  color: var(--wa-color-brand-on-normal);
"></span>
```

## 自定义 {{ title }}

覆盖设计令牌会更新引用它的每个组件和样式，使样式化和重新样式化您的项目变得轻而易举。要自定义令牌，请在样式中设置其值。在 `:root` 上设置令牌可将其应用到整个项目的全局范围：

```css
:root {
  --wa-font-family-body: 'Inter', sans-serif;
  --wa-border-width-scale: 1.5; /* 按比例缩放所有边框宽度 */
}
```

要自定义适应亮色和暗色模式的令牌，请将令牌限定在 `.wa-light`、`.wa-dark` 以及可选的 `.wa-invert` 范围内：

```css
:root,
.wa-light,
.wa-dark .wa-invert {
  --wa-color-surface-default: var(--wa-color-neutral-95);
}

.wa-dark,
.wa-invert {
  --wa-color-surface-default: var(--wa-color-neutral-05);
}
```

您还可以将令牌限定在子树而非 `:root` 范围内，这使得您可以对单个部分或元素应用与其余部分不同的主题：

```css
*:state(user-invalid),
*:user-invalid {
  --wa-color-focus: var(--wa-color-danger-60);
  --wa-form-control-border-color: var(--wa-color-danger-border-normal);
  --wa-form-control-label-color: var(--wa-color-danger-on-quiet);
}
```
{% endmarkdown %}
